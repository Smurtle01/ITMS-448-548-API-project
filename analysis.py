# Outdated file, no longer needed...

# analysis.py
#import pandas as pd

# Analyzing the data with the mean, max and minimums.
#def analyze_time_series(df):
#    return {
#        "mean": df["value"].mean(),
#        "max": df["value"].max(),
#        "min": df["value"].min()
#    }

# Analyzing the categories by count and ordering them
#def analyze_categories(df):
#    return df.sort_values(by="count", ascending=False)

#def analyze_correlation(df):
#    return df.corr()

#def analyze_sales(df):
#   df["rolling_avg"] = df["sales"].rolling(5).mean()
#    return df